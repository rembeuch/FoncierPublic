
<div class="container">
  <div class="row">
    <% @flats.each do |flat| %>
      <div class="col-xl-4 col-md-6 col-12">
        <div class="card">
          <%= cl_image_tag flat.photo, height: 200, width: 300, crop: :fill%>
          <h5 class="card-title"><%= flat.title %></h5>
          <div class="card-body">
            <!-- <p class="card-text">...</p> -->
           <%= link_to 'Détails', flat_path(flat), class:'btn btn-ghost'%>
         </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<%#= flat.owner.first_name %> <%#= flat.owner.last_name %>


